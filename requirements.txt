numpy>=1.24
xarray>=2023.1
netCDF4>=1.6
matplotlib>=3.6
# Optional: coastlines on maps. The package runs without it (falls back to a
# plain lat/lon axes).
cartopy>=0.21
# Optional: running the test suite.
pytest>=7.0
